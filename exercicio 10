#include <stdio.h>
int main() {
int n, soma = 0, valor;
printf("Quantos números deseja somar? ");
scanf("%d", &n);
for (int i = 0; i < n; i++) {
printf("Digite o número %d: ", i + 1);
scanf("%d", &valor);
soma += valor;
}
printf("Soma: %d\n", soma);
return 0;
}
